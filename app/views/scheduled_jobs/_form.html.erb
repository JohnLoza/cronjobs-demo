<%= form_with(model: scheduled_job) do |form| %>
  <% if scheduled_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scheduled_job.errors.count, "error") %> prohibited this scheduled_job from being saved:</h2>

      <ul>
        <% scheduled_job.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
